# Migrated from GitLab

- Original location: `gitlab.contact.ci/demos/hub`
- Migrated: 2026-09-18 (wave 1)
- Branch `develop` on GitLab is `staging` here
- Branch `master` on GitLab is `main` here
- Branch renames applied to every migrated repo: `master` -> `main`, `develop` -> `staging`; every other branch keeps its GitLab name
- Merge request history: https://github.com/Contact-Control-Interfaces/gitlab-migration/tree/main/archive/merge-requests/demo-hub
- Checking out old revisions whose `.gitmodules` still point at GitLab: see the "Old submodule URLs" section of https://github.com/Contact-Control-Interfaces/gitlab-migration/blob/main/archive/README.md
